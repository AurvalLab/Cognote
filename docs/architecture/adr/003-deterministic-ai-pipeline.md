# ADR-003：确定性 AI Pipeline

- 状态：Accepted for specification v0.1
- 日期：2026-07-25

## 背景

产品运行时不使用自主 Agent 生成卡片。

## 决策

固定执行质量检查、OCR/Vision、分类、可选检索、生成、Schema 校验、策略校验和版本保存。LLM 无数据库与任意工具权限。

## 结果

可测试、可追踪和可控；牺牲少量动态规划能力，符合 MVP 风险。

## 复审触发条件

只有产品指标、容量数据、隐私要求或已验证的工程约束发生变化时复审；修改前必须提交替代 ADR。
