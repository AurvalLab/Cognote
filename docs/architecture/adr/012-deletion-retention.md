# ADR-012：删除与保留

- 状态：Accepted for specification v0.1
- 日期：2026-07-25

## 背景

单卡删除、媒体删除、撤销授权和账号删除使用不同流程。

## 决策

删除作业覆盖主库、对象、索引、队列和导出；墓碑与备份延迟窗口明确保留。

## 结果

物理删除不是瞬时事务，需要可审计后台作业和透明状态。

## 复审触发条件

只有产品指标、容量数据、隐私要求或已验证的工程约束发生变化时复审；修改前必须提交替代 ADR。
