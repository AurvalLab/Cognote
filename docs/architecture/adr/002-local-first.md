# ADR-002：Local-First 语义

- 状态：Accepted for specification v0.1
- 日期：2026-07-25

## 背景

无账号、无网络时仍可创建、编辑、删除、恢复和浏览本地数据。

## 决策

Drift 是 UI 即时事实源；本地事务成功即创建成功；云 AI、结构同步和媒体备份独立授权。

## 结果

需要本地迁移、持久队列和恢复测试，但避免把云服务可用性传递给记录行为。

## 复审触发条件

只有产品指标、容量数据、隐私要求或已验证的工程约束发生变化时复审；修改前必须提交替代 ADR。
