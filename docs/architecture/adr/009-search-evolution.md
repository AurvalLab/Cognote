# ADR-009：搜索渐进演进

- 状态：Accepted for specification v0.1
- 日期：2026-07-25

## 背景

搜索按时间线/过滤、FTS、语义搜索、带引用问答的顺序上线。

## 决策

pgvector 只有在真实找回问题证明需要时启用；Embedding 仅对接受卡片生成。

## 结果

降低早期成本与评测负担，可能延后展示性较强的 AI 问答。

## 复审触发条件

只有产品指标、容量数据、隐私要求或已验证的工程约束发生变化时复审；修改前必须提交替代 ADR。
