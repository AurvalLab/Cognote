# ADR-001：模块化单体

- 状态：Accepted for specification v0.1
- 日期：2026-07-25

## 背景

MVP 服务端采用 FastAPI 模块化单体。

## 决策

共享部署和数据库事务边界；模块通过应用服务交互，禁止跨模块直接操作 Repository。

## 结果

降低部署、分布式事务和契约漂移成本；未来只有出现独立扩缩容或团队边界证据时才拆服务。

## 复审触发条件

只有产品指标、容量数据、隐私要求或已验证的工程约束发生变化时复审；修改前必须提交替代 ADR。
