name: cognote
description: "Cognote Android application."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.7
  drift: ^2.34.2
  flutter:
    sdk: flutter
  image: ^4.8.0
  path: ^1.9.1
  path_provider: ^2.1.6
  sqlite3: ^3.5.0
  uuid: ^4.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.15.1
